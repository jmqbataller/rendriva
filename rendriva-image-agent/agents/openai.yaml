interface:
  display_name: Rendriva
  short_description: Professional batches with strict product fidelity
  default_prompt: Use $rendriva-image-agent to generate a professional batch of separate
    images without a collage while preserving supplied product fabric, texture, and
    logos exactly.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true

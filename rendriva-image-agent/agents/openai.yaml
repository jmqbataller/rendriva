interface:
  display_name: Rendriva
  short_description: Reference-branded batches with strict fidelity
  default_prompt: Use $rendriva-image-agent to generate a professional batch of separate
    images without a collage, derive the brand palette from supplied references, and
    preserve product fabric, texture, and logos exactly.
  icon_small: assets/icon.svg
  icon_large: assets/icon.svg
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
